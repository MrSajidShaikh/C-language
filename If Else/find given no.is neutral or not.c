#include<stdio.h>
#include<conio.h>
main()
{
	int a;
	
	printf("enter any value:");
	scanf("%d",&a);
	
	if(a==0)
	{
		printf("'%d'it is a neutral number",a);
	}
	else
	{
		if(a>0)
		{
			printf("it's not a neutral number.",a);
		}
		
		else
		{
			printf("it's not a neutral number.",a);
		}
	}
}